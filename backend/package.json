{
  "name": "bujufix-backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/app.js --exec  \"npm run lint && node\"",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js  --fix"
  },
  "keywords": [],
  "author": "René Gentzen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "sequelize": "^6.13.0",
    "morgan": "^1.10.0",
    "cors": "^2.8.5",
    "body-parser": "^1.19.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "eslint": "^8.6.0"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "node": true,
      "es6": true
    },
    "extends": [
      "eslint:recommended"
    ],
    "rules": {},
    "parserOptions": {
    }
  }
}
